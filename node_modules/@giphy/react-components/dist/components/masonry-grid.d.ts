import React, { ReactNode } from 'react';
declare type Props = {
    columns: number;
    gutter: number;
    useTransform?: boolean;
    children: ReactNode;
    itemHeights: number[];
    itemWidth: number;
    columnOffsets?: number[];
};
declare const _default: React.MemoExoticComponent<({ columns, gutter, useTransform, itemWidth, itemHeights, children, columnOffsets, }: Props) => JSX.Element>;
export default _default;
