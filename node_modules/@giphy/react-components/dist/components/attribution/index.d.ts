/// <reference types="react" />
import { IGif } from '@giphy/js-types';
declare type Props = {
    gif: IGif;
    className?: string;
    onClick?: (gif: IGif) => void;
};
declare const Attribution: {
    ({ gif, className, onClick }: Props): JSX.Element | null;
    className: string;
};
export default Attribution;
