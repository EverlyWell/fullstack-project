/// <reference types="react" />
declare type Props = {
    className?: string;
    placeholder?: string;
    clear?: boolean;
};
declare const SearchBar: {
    ({ className, placeholder, clear }: Props): JSX.Element;
    className: string;
};
export default SearchBar;
