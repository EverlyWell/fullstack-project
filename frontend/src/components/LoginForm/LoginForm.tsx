
import React from 'react';
import StyledComponents from './LoginFormStyled';

const {
    Container,
} = StyledComponents;


const LoginForm = () => {
    return (
        <Container />
    );
};

export default LoginForm;
